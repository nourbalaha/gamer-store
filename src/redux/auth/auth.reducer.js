// Initial State
const initial_state = {
  
}

// Reducer
function authReducer (state = initial_state, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default authReducer;